{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Solver",
            "type": "dotnet",
            "request": "launch",
            "projectPath": "${workspaceFolder}/Runner/Runner.csproj"
        }

    ]
}
